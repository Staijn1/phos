import {NavigationbarComponent} from './navigationbar.component';

describe('NavigationbarComponent', () => {
    let sut: NavigationbarComponent;

    beforeEach(() => {
        // const electronServiceMock = jasmine.createSpyObj('ElectronService', ['isElectron']);
        // const connectionMock = jasmine.createSpyObj('ConnectionService', ['increaseSpeed', 'decreaseSpeed', 'increaseBrightness', 'decreaseBrightness']);
        // const routerMock = jasmine.createSpyObj('Router', ['navigate']);
        // sut = new NavigationbarComponent(electronServiceMock, connectionMock, routerMock);
    });
});
