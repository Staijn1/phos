export type GeneralSettings = {
  initialColor: boolean;
  colors: string[];
  chroma: boolean;
  theme: string;
  darkmodeEnabled: boolean
};

export type RGBObject = {
  r?: number;
  g?: number;
  b?: number;
}
